import EditorCanvas from '@/components/flow01/EditorCanvas';

// Canvas 360 — flow-01 assembly (the live Playbooks editor, empty state).
export default function CanvasPage() {
  return <EditorCanvas />;
}
